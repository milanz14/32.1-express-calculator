const mean = () => {
    // average
};

const median = () => {
    // value in the middle of the data set
};

const mode = () => {
    // values that occur the most frequently
};

module.exports = {
    mean,
    median,
    mode,
};
